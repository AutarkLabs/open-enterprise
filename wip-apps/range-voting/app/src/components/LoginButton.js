import React from 'react'
import { Button } from '@aragon/ui'

class LoginButton extends React.Component {
  render () {
    return (
      <Button mode="strong">Login</Button>
    )
  }
}

export default LoginButton;
